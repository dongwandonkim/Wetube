//- capsulized videoBlock to use wherever I wanted to use
mixin videoBlock(video = {})
    .videoBlock
        video.videoBlock__thumbnail(src=video.videoFile, controls)
        h4.videoBlock__title= video.title
        h6.videoBlock__view= video.views